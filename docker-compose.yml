version: '3.9'

services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.52.0-jammy

    working_dir: /app

    volumes:
      - ./:/app

    command: npx playwright test

    stdin_open: true
    tty: true
